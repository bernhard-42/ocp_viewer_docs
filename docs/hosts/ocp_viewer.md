# ocp_viewer

The standalone viewer, `python -m ocp_viewer`. To be written: startup and CLI flags, settings storage, ports.

Until then, see the [repository documentation](https://github.com/bernhard-42/ocp-viewer#readme).
